#!/usr/bin/env bash

# Create RSA key pair with specified parameters
ssh-keygen -t rsa -b 4096 -N "betty" -C "school" -f "school"

# Rename the private key file to have the correct extension
mv school school.pem

echo "RSA key pair created successfully."

